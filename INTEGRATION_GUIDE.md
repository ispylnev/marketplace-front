# 🔗 Интеграция фронтенда с бекендом

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Настройка](#настройка)
3. [API клиент](#api-клиент)
4. [Примеры использования](#примеры-использования)
5. [Запуск приложений](#запуск-приложений)

---

## 🚀 Быстрый старт

### 1. Установите зависимости фронтенда

```bash
cd vega-frontend
npm install
```

### 2. Настройте переменные окружения

Создайте файл `.env` в корне проекта `vega-frontend`:

```env
VITE_API_BASE_URL=http://localhost:8081
```

### 3. Запустите бекенд

```bash
cd vega-back
./gradlew :vega-auth:bootRun
```

Бекенд будет доступен на **http://localhost:8081**

### 4. Запустите фронтенд

```bash
cd vega-frontend
npm run dev
```

Фронтенд будет доступен на **http://localhost:5173**

---

## ⚙️ Настройка

### Переменные окружения

Фронтенд использует переменные окружения с префиксом `VITE_`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_BASE_URL` | URL бекенда API | `http://localhost:8081` |

### CORS

Бекенд настроен для работы с фронтендом. Разрешенные origins:
- `http://localhost:5173` (Vite dev server)
- `http://localhost:3000` (React dev server)
- `http://localhost:5174` (альтернативный порт)

---

## 🔌 API клиент

### Структура

```
src/
├── api/
│   ├── client.ts      # HTTP клиент (axios)
│   └── auth.ts        # API для аутентификации
├── config/
│   └── api.ts         # Конфигурация API
└── pages/
    ├── Login.tsx      # Страница входа
    └── Register.tsx   # Страница регистрации
```

### Использование API клиента

```typescript
import { authApi } from './api/auth';

// Регистрация
const response = await authApi.register({
  email: 'user@example.com',
  password: 'password123',
  firstName: 'Иван',
  lastName: 'Иванов',
});

// Вход
const response = await authApi.login({
  email: 'user@example.com',
  password: 'password123',
});

// Получение текущего пользователя
const user = await authApi.getCurrentUser();
```

### Автоматическое управление токенами

API клиент автоматически:
- ✅ Добавляет токен к каждому запросу
- ✅ Сохраняет токены в localStorage
- ✅ Обрабатывает ошибки 401 (неавторизован)
- ✅ Поддерживает refresh токены

---

## 📝 Примеры использования

### Регистрация пользователя

```typescript
import { authApi } from '../api/auth';

const handleRegister = async () => {
  try {
    const response = await authApi.register({
      email: 'user@example.com',
      password: 'password123',
      firstName: 'Иван',
      lastName: 'Иванов',
      middleName: 'Иванович',
      phone: '+79991234567',
    });
    
    console.log('Токен сохранен автоматически');
    console.log('Пользователь:', response.user);
  } catch (error) {
    console.error('Ошибка регистрации:', error);
  }
};
```

### Вход пользователя

```typescript
import { authApi } from '../api/auth';

const handleLogin = async () => {
  try {
    const response = await authApi.login({
      email: 'user@example.com',
      password: 'password123',
    });
    
    console.log('Вход выполнен успешно');
    console.log('Пользователь:', response.user);
  } catch (error) {
    console.error('Ошибка входа:', error);
  }
};
```

### Защищенные запросы

После входа токен автоматически добавляется к каждому запросу:

```typescript
import { authApi } from '../api/auth';

// Этот запрос автоматически включает токен
const user = await authApi.getCurrentUser();
```

### Выход из системы

```typescript
import { authApi, apiClient } from '../api/auth';

const handleLogout = async () => {
  const refreshToken = apiClient.getRefreshToken();
  if (refreshToken) {
    await authApi.logout(refreshToken);
  }
  // Токены автоматически очищены
};
```

---

## 🚀 Запуск приложений

### Вариант 1: Запуск в отдельных терминалах

**Терминал 1 - Бекенд:**
```bash
cd vega-back
./gradlew :vega-auth:bootRun
```

**Терминал 2 - Фронтенд:**
```bash
cd vega-frontend
npm run dev
```

### Вариант 2: Использование npm scripts (рекомендуется)

Создайте скрипт для одновременного запуска:

**package.json (в корне проекта):**
```json
{
  "scripts": {
    "dev:backend": "cd vega-back && ./gradlew :vega-auth:bootRun",
    "dev:frontend": "cd vega-frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
```

Затем:
```bash
npm install concurrently
npm run dev
```

### Вариант 3: Docker Compose (для будущего)

Создайте `docker-compose.yml` для одновременного запуска всех сервисов.

---

## 🧪 Тестирование интеграции

### 1. Проверка подключения

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверка доступности API
fetch('http://localhost:8081/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ email: 'test@test.com', password: 'test' })
})
  .then(res => console.log('Статус:', res.status))
  .catch(err => console.error('Ошибка:', err));
```

### 2. Тест регистрации через UI

1. Откройте http://localhost:5173/register
2. Заполните форму
3. Отправьте запрос
4. Проверьте консоль браузера и Network tab

### 3. Проверка токенов

После успешного входа проверьте localStorage:

```javascript
// В консоли браузера
localStorage.getItem('accessToken');
localStorage.getItem('refreshToken');
```

---

## 🔧 Troubleshooting

### Ошибка CORS

**Проблема:** `Access to fetch at 'http://localhost:8081' from origin 'http://localhost:5173' has been blocked by CORS policy`

**Решение:**
1. Убедитесь, что бекенд запущен
2. Проверьте, что порт фронтенда в списке разрешенных (5173, 3000, 5174)
3. Перезапустите бекенд

### Ошибка подключения

**Проблема:** `Network Error` или `ERR_CONNECTION_REFUSED`

**Решение:**
1. Убедитесь, что бекенд запущен на порту 8081
2. Проверьте `VITE_API_BASE_URL` в `.env`
3. Проверьте firewall/антивирус

### Токен не сохраняется

**Проблема:** Токен не добавляется к запросам

**Решение:**
1. Проверьте localStorage в DevTools
2. Убедитесь, что используете `authApi` для входа/регистрации
3. Проверьте, что токен не истек

---

## 📚 Дополнительные ресурсы

- [Axios Documentation](https://axios-http.com/)
- [React Router](https://reactrouter.com/)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)

---

## ✅ Чеклист интеграции

- [x] Установлен axios
- [x] Создан API клиент
- [x] Настроены переменные окружения
- [x] Обновлен CORS на бекенде
- [x] Созданы страницы Login/Register
- [x] Добавлены маршруты в App.tsx
- [ ] Тестирование полного цикла
- [ ] Добавление обработки ошибок
- [ ] Добавление loading состояний
- [ ] Интеграция с другими API

